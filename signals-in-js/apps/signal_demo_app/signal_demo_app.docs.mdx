---
description: 'A signal_demo_app app.'
labels: ['app', 'react']
---

A React application for signal_demo_app. 

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use signal_demo_app 
```

To run the app locally, run the following:

```
bit run signal_demo_app 
```
